(function(){
"use strict";
const API_BASE="https://ct-report-generator.fairpeace.workers.dev";const TOKEN_KEY="ct_map_session_token";const TOKEN_EXPIRY_KEY="ct_map_session_expires";let secureAuthPromise=null;const priorFetch=window.fetch.bind(window);function sessionToken(){return String(sessionStorage.getItem(TOKEN_KEY)||"");}
async function secureWorkerAuth(){const username=String(document.getElementById("access-username")?.value||"").trim().toLowerCase();const password=String(document.getElementById("access-password")?.value||"");if(!username||!password)return null;if(secureAuthPromise)return secureAuthPromise;secureAuthPromise=(async()=>{try{const response=await priorFetch(API_BASE+"/auth-login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password,client_time:new Date().toISOString(),user_agent:navigator.userAgent})});const payload=await response.json().catch(()=>({}));if(response.ok&&payload.session_token){sessionStorage.setItem(TOKEN_KEY,String(payload.session_token));if(payload.expires_at){sessionStorage.setItem(TOKEN_EXPIRY_KEY,String(payload.expires_at));}return String(payload.session_token);}}catch(error){console.warn("Secure Worker authentication unavailable:",error);}finally{setTimeout(()=>{secureAuthPromise=null;},600);}return null;})();return secureAuthPromise;}
document.addEventListener("click",event=>{if(event.target?.id==="access-button"){secureWorkerAuth();}},true);document.addEventListener("keydown",event=>{if(event.key==="Enter"&&event.target?.id==="access-password"){secureWorkerAuth();}},true);
window.fetch=async function(input,init){const url=typeof input==="string"?input:String(input?.url||"");const options=init?{...init}:{};if(/\/login(?:\?|$)/.test(url)&&secureAuthPromise){try{await secureAuthPromise;}catch(_){}}const token=sessionToken();if(token&&(/\/login(?:\?|$)/.test(url)||/\/report(?:\?|$)/.test(url))){const headers=new Headers(options.headers||{});headers.set("X-Session-Token",token);options.headers=headers;}return priorFetch(input,options);};
})();
